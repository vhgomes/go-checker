version: "3.8"

services:
  redis:
    image: redis:7.2
    container_name: redis_service
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data

volumes:
  redis_data:
